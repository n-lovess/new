CHIP DecoderSegment201830364 {
    IN A, B, C, D;    
    OUT d;           
    
    PARTS:
    AND(a= A, b= B, out= temp1);  
    AND(a= A, b= C, out= temp2);  
    AND(a= B, b= D, out= temp3); 
    OR(a= temp1, b= temp2, out= d); 
}