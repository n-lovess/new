CHIP DecoderSegment201830364 { 
    IN A, B, C, D; 
    OUT d;  

    PARTS:

    NOT(a=A, out=A');  
    NOT(a=D, out=D');  


    AND(a=A', b=B, out=term1); 
    AND(a=A, b=C, out=term2);  
    AND(a=B, b=D', out=term3);  


    OR(a=term1, b=term2, out=or1);  
    OR(a=or1, b=term3, out=d);
}